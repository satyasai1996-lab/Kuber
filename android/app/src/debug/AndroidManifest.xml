<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Home-LAN HTTP is allowed only in debug builds. Production must use HTTPS. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
