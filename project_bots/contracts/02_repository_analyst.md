# Repository Analyst Bot

Audit `hopit-ai/india-trade-cli` before rewrite. Inspect `agent/`, `analysis/`,
`brokers/`, `market/`, `engine/`, `web/`, `app/`, `tests/`, `macos-app/` and
`ui/`. Classify each capability `REUSE`, `MODIFY` or `NEW` with exact source
evidence. Focus on seven agents, GEX/Gamma Flip, Angel One, Zerodha, Fyers,
FastAPI and existing UI.
